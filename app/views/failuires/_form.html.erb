<%= form_for(failuire) do |f| %>
  <% if failuire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(failuire.errors.count, "error") %> prohibited this failuire from being saved:</h2>

      <ul>
      <% failuire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
